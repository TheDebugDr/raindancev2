"""Shared UI primitives: design tokens and the small set of components every
lifecycle stage is built from. Plugins import from here rather than inventing
their own CSS, so the app reads as one surface."""
